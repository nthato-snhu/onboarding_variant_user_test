# =============================================================================
# Database Configuration (for transcript storage)
# =============================================================================
# For local development with Docker PostgreSQL:
POSTGRES_URL=postgresql://postgres:postgres@localhost:5432/onboarding_transcripts

# For production (Vercel), use Neon or Vercel Postgres connection string:
# POSTGRES_URL=postgresql://user:password@host.region.neon.tech/dbname

# =============================================================================
# Admin Portal Configuration
# =============================================================================
# Password to access /admin/transcripts dashboard
ADMIN_PASSWORD=2026@MAS

# =============================================================================
# LLM Provider Configuration
# =============================================================================
# Set LLM_MODEL to choose your provider. The model string prefix determines
# which credentials are used (unused credentials are ignored).
#
# Examples:
#   LLM_MODEL=gpt-4o                                        -> uses OPENAI_API_KEY
#   LLM_MODEL=azure_openai:mas-gs-no-guardrails-gpt-4o      -> uses AZURE_OPENAI_*
#   LLM_MODEL=anthropic:claude-3-5-sonnet-20241022           -> uses ANTHROPIC_API_KEY

LLM_MODEL=azure_openai:mas-gs-no-guardrails-gpt-4o

# OpenAI
# OPENAI_API_KEY=sk-your-openai-api-key

# Azure OpenAI
AZURE_OPENAI_API_KEY=your-azure-api-key
AZURE_OPENAI_ENDPOINT=https://humansystems-openai.openai.azure.com/
OPENAI_API_VERSION=2024-12-01-preview

# Anthropic
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key
